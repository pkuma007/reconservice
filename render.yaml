services:
  - type: web
    name: reconservice
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/reconservice-1.0-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8080
    plan: free
